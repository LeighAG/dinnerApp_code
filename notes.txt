let dates = "05-08-99-08-09-55";

let newDates = dates.replace('-', '/').replace('-', '/')
.replace('-', ' to ').replace('-', '/').replace('-', '/')

console.log(newDates)

or

let datesArr = dates.split("-");
let dates1 = datesArr.slice(0,3).join("/")
let dates2 = datesArr.slice(3,6).join("/")
let fullDate = `Dinner plan for ${dates1} to ${dates2}`
console.log( fullDate)

//////problems
click logout- needs to redirect to login page

//
defaults
imr - import react from react

